# Markdown & LaTeX Quick Reference

## Headings

```markdown
# Heading 1
## Heading 2
### Heading 3
```

## Text Formatting

```markdown
**bold**
*italic*
```

## Bullets

```markdown
- Item
  - Sub-item
```

## Code

### Inline code:
```markdown
`lm()`
```
### Code block:

```markdown
```r
```stata
```python
```

## Math

### Inline:
```markdown
$R^2$
```
### Block equation:
```markdown
$$
Y_i = \beta_0 + \beta_1X_i + \epsilon_i
$$
```
 
### Common Math Syntax
|Syntax| Result |
|---|---|
|\beta|$\beta$|
|\epsilon|	$\epsilon$|
|\alpha	|$\alpha$|
| \Delta | $\Delta$
|X_i	|$X_i$|
|X_{it}	|$X_{it}$|
|\hat{\beta}|	$\hat{\beta}$|
|\bar{X}	|$\bar{X}$|
|\sum	|$\sum$|
|\in	|$\in$|
|\neq	|$\neq$|
|\leq	|$\leq$|
|\geq	|$\geq$|
|||
|\qquad |space|
|\sqrt{} |$\sqrt{}$|





